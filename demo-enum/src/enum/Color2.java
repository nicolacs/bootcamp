public enum Color2 {
  // enum values (objects)
  RED, BLUE; // 2 objects - static final (全局入面唯一既OBJ)

  // 以下係正常class要寫既野. Normal Class Section
  // attributes (can be no attribute)
  // constructor
  // methods

  // Meaning of enum:
  
  // JVM will help create the Color2 OBJ (RED, BLUE)
  // As a user, you cannot create the enum OBJ by yourself.
  
}
